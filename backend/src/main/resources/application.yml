server:
  port: 8080

spring:
  application:
    name: trading-signals
  datasource:
    url: jdbc:postgresql://db:5432/trading
    username: trading
    password: trading

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration